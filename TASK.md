# Task: Sometimes we don't want to create a whole new worktree, we just want to ask the AI something about the code, but not necessarily modify it, run it and create a PR. We should be able to do that too

## Project
cli

## Branch
ai-only

## Instructions

Please implement the following task:

Sometimes we don't want to create a whole new worktree, we just want to ask the AI something about the code, but not necessarily modify it, run it and create a PR. We should be able to do that too

## When done

1. Make sure all changes are committed
2. Run any relevant tests to verify your changes work
3. Create a file called `.task-done` in the project root: `touch .task-done`

This signals the task runner that you've completed the work.
